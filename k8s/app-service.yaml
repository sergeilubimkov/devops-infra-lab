apiVersion: v1
kind: Service
metadata:
  name: app-service
  namespace: default
  labels:
    app: hello-world
spec:
  type: NodePort
  selector:
    app: hello-world
  ports:
    - name: http
      port: 80
      targetPort: 9000
      nodePort: 30080
      protocol: TCP
